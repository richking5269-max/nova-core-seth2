{
  "name": "nova-core-seth2",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev .",
    "build": "node scripts/build.mjs",
    "deploy": "node scripts/build.mjs && cd dist && wrangler pages deploy . --project-name=novacore --no-bundle",
    "check": "node --check app.js && node --check admin/admin.js"
  },
  "devDependencies": {
    "rollup": "4.62.3",
    "wrangler": "4.115.0"
  }
}
